#Set the MS SQL Server instance to use when executing SQLCMD.exe
DBServer=.

#Set the name of the database in the MS SQL Server instance where
#the SQLChangeFramework objects will be created.
DBName=SQLChange